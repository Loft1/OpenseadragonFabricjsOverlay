{
  "name": "@loft1/osd-fabricjs-overlay",
  "version": "2.0.0",
  "description": "An OpenSeadragon plugin that adds fabricjs canvas overlay capability.",
  "main": "index.js",
  "devDependencies": {
    "tslint": "^5.14.0",
    "typescript": "latest"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && cp package.json build/package.json",
    "lint": "tslint -p ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Loft1/openseadragon-fabricjs-overlay.git"
  },
  "author": "Alexey Tikhonov",
  "license": "BSD-3-Clause",
  "dependencies": {
    "openseadragon": "^2.4.0"
  }
}
